using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SelfVariables : MonoBehaviour
{
    // Start is called before the first frame update
    public HealthBar healthBar;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
